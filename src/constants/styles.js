// SPDX-License-Identifier: GPL-3.0-only

export const LINE_STYLES = Object.freeze({
    SOLID: 'solid',
    DASHED: 'dashed',
    DOTTED: 'dotted'
});

export const LINK_MARKERS = Object.freeze({
    NONE: 'none',
    ARROW: 'arrow',
    DOT: 'dot'
});

export const HATCH_TYPES = Object.freeze({
    HATCH_45: 'hatch-45',
    HATCH_135: 'hatch-135',
    HATCH_CROSS: 'hatch-cross',
    HATCH_DOTS: 'hatch-dots'
});

export const LEGEND_ENTRY_TYPES = Object.freeze({
    LINE: 'line',
    ARROW: 'arrow',
    HATCH: 'hatch'
});

export const EDGE_ARROW_TYPES = Object.freeze({
    FILLED: 'filled',
    OPEN: 'open'
});

export const LABEL_POSITIONS = Object.freeze({
    ABOVE: 'above',
    BELOW: 'below',
    CENTER: 'center'
});

export const MEASUREMENT_LABEL_POSITIONS = Object.freeze({
    TOP: 'top',
    BOTTOM: 'bottom',
    CENTER: 'center'
});

export const SPACING_MODES = Object.freeze({
    PITCH: 'pitch',
    GAP: 'gap'
});

export const LABEL_JUSTIFY = Object.freeze({
    START: 'start',
    MIDDLE: 'middle',
    END: 'end'
});
